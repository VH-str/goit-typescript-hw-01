// Змінна  (union type):
let value: string | number;
value = "Hello";
value = 42;

// Змінна 'enable' або 'disable' (literal type):
let status: "enable" | "disable";
status = "enable";
status = "disable";
